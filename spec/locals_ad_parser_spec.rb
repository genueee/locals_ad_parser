require 'spec_helper'

describe LocalsAdParser do
  it 'has a version number' do
    expect(LocalsAdParser::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
